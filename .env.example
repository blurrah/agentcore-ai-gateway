# Vercel dashboard -> AI Gateway -> API keys
AI_GATEWAY_API_KEY=

# examples/09_agent_vercel_connect_mcp.py
# Project-scoped Vercel access token used by the external AgentCore runtime.
VERCEL_TOKEN=
# Connector UID returned by `vercel connect create`, for example oauth/linear.
VERCEL_CONNECTOR_UID=
# Streamable HTTP endpoint registered by that connector.
MCP_SERVER_URL=
